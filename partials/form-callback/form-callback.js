jQuery(document).ready(function ($) {

	//form-callback popup init (VENDOR: jquery-popup-overlay)
	$("#js-form-callback").popup({
		color: '#285c96',
		opacity: .8,
		transition: '.5s',
		scrolllock: false
	});

});
