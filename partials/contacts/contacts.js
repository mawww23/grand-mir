jQuery(document).ready(function ($) {

	//map-lightbox popup init (VENDOR: jquery-popup-overlay)
	$("#js-map-lightbox").popup({
		color: '#285c96',
		opacity: .8,
		transition: '.5s',
		scrolllock: false
	});

});
